use Test;
use lib 'lib';

plan 1;

use-ok "Chip8";

# use Chip8::CPU;
# my $file = open 'roms/test_opcode.ch8', :bin;
# my $contents = $file.slurp;
# $file.close;

# my $cpu = CPU.new();
# $cpu.init($contents);

# ok $cpu.memory.elems eq 4096, 'Memory size is ok';
# ok $cpu.v.elems eq 16, 'Register size is ok';

# $cpu.emulate;
done-testing;